__pycache__/
*.pyc
*.pyo
.DS_Store
keys.env
*.env

# Ignore all generated report files inside reports/
reports/*

# Keep the delta snapshot so GitHub Actions carries it between daily runs
!reports/latest_analysis.json

# docs/ is intentionally tracked (contains CNAME + deployed index.html)
!docs/
